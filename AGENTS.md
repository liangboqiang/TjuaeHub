# TjuaeHub 项目规范

本文档适用于所有人类贡献者和 AI 智能体。修改仓库前应先阅读本规范。

## 项目职责

TjuaeHub 负责维护 Tjuae 扩展清单、清单模式、安装脚本和可发布扩展包。它不承载
TjuaeUI、TjuaeCore 或 TjuaeCLI 的运行时代码。

主要目录：

- `extensions/`：已经启用并参与正式分发的扩展。
- `pending/`：尚未进入正式分发的候选扩展。
- `schemas/`：唯一受支持的扩展清单 JSON Schema。
- `kits/`：扩展开发和本地验证工具。
- `scripts/quality/`：品牌、清单和构建产物门禁。
- `tests/`：模式与构建行为测试。

## 扩展契约

- 扩展清单文件只能命名为 `tjuae-extension.json`。
- 扩展包名必须使用 `tjuaeext-` 前缀。
- 引擎字段只能使用 `engine.tjuae`，不得增加旧名称别名。
- 不得为旧文件名、旧环境变量、旧包名前缀或旧引擎键增加兼容回退。
- `contributes.*` 可以内联声明，也可以使用 `$file:` 引用相对 `.json` 文件。
- `$file:` 路径必须使用正斜杠，并拒绝绝对路径、盘符、UNC、反斜杠、空路径段、
  前导空白和 `..` 路径穿越。

## 品牌与文案

- 产品身份统一使用 `Tjuae`、`TjuaeUI` 和 `TjuaeHub`。
- 扩展作者统一使用 `Tjuae`。
- 面向用户的扩展说明、安装日志和错误提示使用简体中文；命令、协议字段和第三方
  产品专名保持原样。
- 活跃源码和分发产物中不得出现旧产品品牌。许可证要求的历史归属只能保留在
  `LICENSE` 和 `UPSTREAM.md`。
- 不得加入广告、赞助、推广活动或无法由本仓库验证的“官方背书”文案。

## 代码与安全

- TypeScript 必须通过严格类型检查，不使用无理由的 `any`。
- 安装脚本不得打印密钥、令牌或完整凭据。
- 下载第三方二进制或软件包时应使用公开、可审计的上游地址，并明确失败退出。
- 不得执行未经约束的路径删除、动态代码下载后静默执行或绕过模式验证。
- 构建产物必须可由仓库源码重新生成；不要手工编辑 `dist/`。

## 验证

完成改动后必须执行：

```bash
bun run verify
```

该命令必须同时通过格式、品牌扫描、14 份扩展清单、TypeScript 类型检查、测试、
扩展构建和 `dist/` 产物验证。任何一步失败都属于阻断问题。

## 提交

- 提交信息使用 Conventional Commit：`<type>(<scope>): <subject>`。
- 不提交密钥、临时目录、依赖缓存或本地构建残留。
- 不添加 AI 署名或 `Co-Authored-By`。
- 未经用户明确授权不得推送；获得授权后也必须先通过完整门禁。
